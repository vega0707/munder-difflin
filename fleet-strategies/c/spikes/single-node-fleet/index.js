/**
 * Spike: in-memory single-node fleet (no CLI).
 * Goal: register runtime → create task → claim → complete.
 * Run: node --experimental-strip-types spikes/single-node-fleet/index.ts
 * (or rewrite to .mjs as you prefer)
 */
console.log('TODO: implement in-memory RuntimeRegistry + ClaimService');
console.log('See docs/PROTOCOL.md');
